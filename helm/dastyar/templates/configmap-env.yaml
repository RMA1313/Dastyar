kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "dastyar.fullname" $ }}-configenv
data:
  {{- if (index .Values "dastyar-rag-api" "enabled") }}
  RAG_API_URL: http://{{ include "rag.fullname" (index .Subcharts "dastyar-rag-api") | lower }}.{{ .Release.Namespace | lower }}.svc.cluster.local:8000 
  {{- end }}
  {{- if and (not (dig "configEnv" "MEILI_HOST" "" .Values.dastyar)) .Values.meilisearch.enabled }}
  MEILI_HOST: http://{{ include "meilisearch.fullname" .Subcharts.meilisearch }}.{{ .Release.Namespace | lower }}.svc.cluster.local:7700
  {{- end }}
  {{- if and (not (dig "configEnv" "MONGO_URI" "" .Values.dastyar)) .Values.mongodb.enabled }}
  MONGO_URI: mongodb://{{ include "mongodb.service.nameOverride" .Subcharts.mongodb }}.{{ .Release.Namespace | lower }}.svc.cluster.local:27017/Dastyar
  {{- end }}
  {{- if .Values.dastyar.configEnv }}
  {{- toYaml .Values.dastyar.configEnv | nindent 2 }}
  {{- end }}